# 瓦斯预置案例本机证据落点

该目录只定义 `gas-preset-live.spec.ts` 的本机真实 SDK 输出位置，不保留历史重复截图。

测试通过 `seed-gas` 使用内置瓦斯源；生成目录使用 `run_id`，身份文件登记 40 位 `git_commit`。`run-*` 默认被 Git 忽略。正式证据必须使用真实 SuperMap3D 渲染和可追溯成果身份，并如实保留稀疏样本与负 R² 边界。当前答辩证据统一收敛到 `docs/evidence/v0.9.0/`。
