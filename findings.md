# Findings

## 2026-08-11 初始审计

- 当前分支 `feat/v0.9.0-visual-product`，HEAD `07bd4d2`。
- 工作区已有未提交截图、`v070RenderGates.ts`、`ResultGridEvidence.vue` 与证据目录；均视为外部改动，不纳入本轮提交。
- 唯一需求入口已扩展为 23 项问题，涵盖 UI、图表、三维、导航和已取消范围。
- `AppHeader.vue` 当前仍是“品牌 + 首页 + 案例上下文 + 服务状态 + 新建 + 回收站”旧结构，与设计要求的四个产品主导航不一致。
- `CaseStageNav.vue` 已是 button，但当前仅发出 `navigate`；需继续追踪父组件是否真正条件渲染阶段内容及 URL 状态。
- 路由中答辩模式已移除，未知深链回首页。
- `CaseWorkspaceView.vue` 的 `onStageNavigate()` 仍调用 `scrollIntoView()`，四个阶段 section 同时渲染；这是 UI-05 的直接根因。
- `ResultWorkbenchView.vue` 仍显式渲染 `V6ResultTopbar`，与全局 `AppHeader` 形成两套顶部栏；这是 UI-01 的直接根因之一。
- `ResultGridEvidence.vue` 的深度趋势和组件比较均配置两个 value Y 轴，但两个轴都默认绘制 splitLine，且未统一刻度；这是 CHART-01/02 的直接根因。
- 组件图 `grid.left` 固定为 56px，原始支持量可达百万，左轴标签必然裁切；这是 CHART-03 的直接根因。
- 深度与组件图直接使用原始浮点数作为标签和 tooltip，未做单位化格式；这是 CHART-04 的直接根因。
- `NativeVolumePanel.vue` 会把传入的全部 `components` 映射为标注，故 A–H 不一致不是渲染器只能显示三个，而是上层 `sceneComponents` 只传了部分组件；需要继续追踪其 computed 来源。
- 已确认 `ResultWorkbenchView.vue:78-79` 对 `components_preview.rows` 显式执行 `.slice(0, 3)`，而右侧和底部使用完整 rows；这是 3D-01 的直接根因。
- `AppShell.vue` 仍在成果路由隐藏 `AppHeader` 并依赖 `V6ResultTopbar`；需要删除该分叉，使所有主路由使用唯一全局头。
- 用户可见旧词仍存在于比较页、分析类型和组件文案中（“模型对比”），需统一为“模型比较”。
- 全局字号 token 仍为 11/12/13/15px，和需求中的辅助文字不小于 12px、正文 14–16px 不一致。
- CandidateComparison、Experiment、Upload、Wizard、ProfessionalAnalysis/Diagnosis、Trash 各自保留 720/980/1080/1100px 等不同 max-width，是页面留白和左右基线不统一的主要来源。
- 前端完整基线 `npm --prefix web run test:unit` 于当前 HEAD 退出码 0，可进入 TDD 修复；测试存在既有 ECharts DOM 尺寸警告，但无失败。
- `ResultDepthBin` 和 `ResultComponentPreview` 类型已提供图表所需全部字段，可把 option 构建提取成无副作用纯函数，无需改变后端合同。

## 2026-08-12 全站逐页 UX 巡检

### 首页与全局导航（真实浏览器 1440×900）

- 顶部导航没有显式“首页”；只有品牌 Logo/产品名链接回首页，用户无法从信息架构上识别首页入口。
- 顶部栏混合了全局入口和依赖当前案例/数据/实验/成果上下文的阶段入口：首页中“模型比较”是禁用文字；案例自动选中后“建模实验/成果空间”才临时可点，导航集合不稳定，禁用项看起来像坏链接。
- 首页案例卡的整块主区域是“选择当前案例”，不会进入案例工作台；真正进入工作台依赖卡片底部较小的“进入案例分析”链接，符合用户反馈“非要点小字才能跳转”。
- 案例卡同时放“选择案例”“进入案例分析”“查看官方成果”三套语义，但视觉只突出当前选中态，用户难以理解单击卡片与底部链接的区别。
- 首页 1440×900 首屏信息密度过高：左侧案例轨、中部完整三维工具栏、右侧四张发现卡、底部证据带同时争夺注意力；中部成果虽是核心，却被技术说明和控制项压缩。
- 首页仍直接暴露 `NetCDF`、`SuperMap3D VoxelGridLayer3D`、`display_anchor_only`、字段名 `RHO/CH4_content` 等开发/数据合同信息，不适合作为面向评委或普通用户的首屏主叙事。
- 右侧“关键发现”卡片文字过长、证据口径直接铺陈，首屏形成密集长段文本；核心结论、可信度与详情层级未分离。

### 案例工作台

- 顶部全局导航、面包屑、圆形返回按钮、四阶段标签、常用操作条、内容区按钮共存，返回与前进路径重复。
- “从这里直接进入下一步，不必先猜阶段标签”是对交互缺陷的解释，不应成为用户界面文案。
- 官方案例的推荐主路径应是查看官方成果/分析，但“新建建模实验”在常用操作中视觉最强，任务优先级倒置。
- “查看官方成果”单独占一整条空卡；下方数据详情又单独占一条卡，首屏大面积空白且没有成果缩略图、数值范围、质量或测点分布摘要。
- 页面仍暴露 `validated`、`local_linear`、`RHO`、数据版本 ID 等技术字段。

### 数据接入、新建案例与数据准备

- “新建案例”与“数据接入”实际都是上传文件入口，标题、说明和动作不同但能力高度重复，用户不知道何时使用哪一个。
- 表单区只有案例名和原生文件控件，桌面首屏极度空旷，缺少支持格式示例、字段预览、模板下载、步骤说明和最近操作反馈。
- 顶部已存在“数据接入”，右侧又固定“导入数据 / 新建建模”，形成同义双入口。
- 已验证的官方数据版本仍可通过深链进入“数据接入与准备”，接口返回 `DATASET_PARSE_FAILED`；页面暴露 case/dataset UUID 和错误代码。应在路由/工作台层阻止不适用入口，或为已验证数据展示只读质量摘要，而不是让用户进入失败页。

### 新建实验

- 三栏布局有基础，但仍暴露数据 UUID、case slug、随机种子等内部字段；缺少面向非算法用户的“推荐方案/高级配置”分层。
- IDW、普通克里金、DSI-like 同层排列，但算法适用条件、成本、输出差异只用短句说明，用户难以决策。
- 空间预览更像规则点阵调试图，缺少轴标题、采样分布语义、选中参数对空间结果的预期影响。
- 页面右下和中下存在未被内容填充的卡片/大面积空区，候选摘要与主提交动作的视觉联系弱。

### 空间结构分析（原专业诊断）

- 页面初始状态只有一张横跨全宽的配置卡和“开始空间结构分析”，大量留白，像未完成页面。
- 文案直接写“普通克里金值提供各向异性证据，IDW 不需要此分析”，但没有回答用户何时必须做、会改变哪些参数、可否跳过。
- “高级设置”是低识别度折叠行，开始按钮与输入条件分离；结果未生成前没有示意图或可理解的输出预览。

### 模型比较

- 页面标题下直接显示数据 UUID；候选表直接展示 `convergence_tolerance`、`hard_constraints`、`init_power` 等代码字段，评审与普通用户无法快速阅读。
- 需要先勾选 2 个候选才能产生价值，但页面没有推荐选择、默认对比或差异摘要，初始态只是窄表格加大片空白。
- “查看”是弱文本动作，未说明查看成果还是实验；比较结果与成果页之间的信息架构连接弱。

### 统计与空间分析中心

- 这是重要展示页，但当前左侧只有 3 个纵向标签，中间一张大图，右侧密集统计卡，视觉中心不明确，右栏底部内容在 900px 视口被截断。
- 高/低阻区域图本质是离散柱块图，缺少空间方位解释和图例叙事，用户很难理解它与三维体的对应关系。
- 阈值来源、NumPy 分位数、样本计数口径等方法说明直接压在图下，喧宾夺主；应该折叠进“方法与口径”。
- 页面同时展示案例 slug、dataset UUID、`analysis.v1`、`RHO` 字段名，产品语言和审计语言没有分层。
- 分析中心与成果工作台是独立路由且视觉结构完全不同，用户无法在分析结论和三维成果之间保持空间/筛选上下文。

### 成果工作台

- 功能最完整，但首屏同时出现全局头、成果摘要条、左侧渲染工具、中央三维、右侧研判、底部多标签分析，信息密度接近上限，缺少“浏览/分析/证据”模式聚焦。
- 技术状态仍大量外露：`local_linear`、`display_anchor_only`、渲染器类名、`volume_coordinate_unit`、`ordinary_kriging` 等。
- “模型评估”“导出分析报告”“规则研判/AI 辅助”“综合分析/切片与异常/模型证据/数据溯源”等多个一级动作并列，层级和使用顺序不清。
- 页面可滚区域多且边界接近，容易出现用户滚动了错误面板；左、右、底部均有自己的内容纵深。

### 模型评估

- 与成果工作台成熟度断层明显：只有一张“基线评估”卡和一句“未生成增强证据”，近 80% 页面空白。
- 指标只是横排数字，没有阈值解释、残差分布、交叉验证、可信度结论或与其他候选的对照；称为“模型评估”名不副实。
- 页面直接显示完整成果 UUID；“基础评估可用”没有给出用户下一步。

### 回收站

- 空态仍保留完整五列表格，造成大面积空表与页面空洞；更适合居中空态和返回/新建动作。
- “首页 / 回收站”面包屑能回首页，反而再次说明顶栏缺少稳定首页入口。

### 横向一致性

- 成果页是满屏高密度工作台；创建、上传、诊断、评估、回收站是窄内容+大留白；工作台和分析中心介于两者之间，像多个产品拼接。
- 多数页面靠面包屑中的“首页”回去，顶部主导航没有稳定首页；返回方式包括品牌、面包屑、圆形返回、文字按钮，规则不统一。
- 同一顶栏同时存在“数据接入”和“导入数据 / 新建建模”；不同页面中“案例工作台/案例分析/成果空间/成果工作台”等名词边界不清。
- 全站普遍使用 12px 辅助文字，深色低对比背景下阅读负担明显；技术 ID 又常使用等宽小字，进一步降低可读性。

### 移动端 390×844 真实浏览器

- 全局头严重拥挤：右侧“导入数据 / 新建建模”在 390px 宽度被压成竖排窄条，覆盖/挤压内容；这是明确的布局缺陷，不只是审美问题。
- 首页案例轨只露出部分案例卡，主动作和服务状态争抢同一行；用户要横向理解案例列表，首屏重点不稳定。
- 案例工作台能纵向阅读，但阶段导航、常用操作和数据概览仍是连续卡片，主动作被推到多个区块之间。
- 成果页移动端中央三维画布仍占据横向布局，工具栏与渲染区同时可见，右侧内容不可见或被推出视口；应明确切换“控制/画布/分析”而非缩小桌面工作台。
- 回收站移动端虽有表格内部滚动，但空表仍只显示前三列，用户看不到空态动作，页面像加载不全。
- 分析中心移动端图表与说明按桌面结构缩放，方法说明会压在首屏下方；没有移动优先的“结论→图表→口径”顺序。

### 运行时错误信号

- 多个深链页面控制台出现 `GET /api/datasets/{dataset_id}/inspection 400 DATASET_PARSE_FAILED`。当前示例数据已验证，用户从工作台/地址栏进入数据准备会看到失败页，说明入口状态约束不足或接口把预置已标准化数据误当上传原始数据解析。

## 2026-08-12 用户截图复核

- 切片模式在三维主舞台下方和底部“切片与异常”各绘制一张相同热力图；前者来自 `SliceAnalysisPanel`，后者来自 `ResultGridEvidence`，重复源于前者同时承担数据控制和可视绘图。
- 切到“分析”焦点后，主舞台仍保留内联切片图，右侧异常列表和底部证据窗同时展开，形成三块相互争夺高度的切片内容；这不是单个 CSS 溢出能可靠修复。
- “成果组成”环图的外侧标签、引导线、底部图例和已有三项数字摘要重复表达，在当前卡片高度内发生碰撞；应保留环图中心语义和下方三项摘要，移除外侧标签与图例。
- 窄视口成果页仍按桌面工具栏+画布双列缩小，首屏只见局部控件与黑色画布；需要断点下切成单列/聚焦式布局，而非继续压缩。
- SDK 右侧竖向控件是相机缩放控件，不是透明度条。必须分别测量滚轮单步和竖条拖动后的相机距离，并在与成果包围盒尺度相关的安全区间内限幅。
- 首页用户项目 `test` 处于未完成数据准备状态，主卡显示“继续数据准备”，但点击无导航；需要同时检查主动作模板绑定和后端 `next_action.url`，并以真实路由跳转作为验收标准。
- 成果网格四模块应改为任务语言：成果概览、切片分析、模型可信度、数据与导出。每模块固定为“结论+关键数值+主图/证据”，技术身份仅留折叠详情。

## 2026-08-13 成果工作台真实浏览器复验

- 当前分支 production build 已覆盖唯一 `8000`，健康检查 `0.9.0 / ok`，PID 129468；复用 `var/geomodeling`，没有新开端口。
- 电阻率正式成果 `7387aa07-...` 在 1440×900 真实 SDK 状态为“已渲染”，无加载遮罩、页面级横纵溢出均为 0。
- 成果组成环图只保留圆环和下方三项摘要；真实截图无外侧标签、引导线或图例碰撞。
- 切片模式只有一张二维热力图：`ge-slice-heatmap=1`、旧 `slice-analysis=0`；三维控制条保留 X/Y/Z、索引与权威坐标；进入模式后自动选中“切片分析”。
- 成果摘要接口不可用时，旧总开关会把已加载的切片证据挡成空态；已按 TDD 拆分可用性，切片热力图/统计/导出独立可用，异常区域单独降级。
- “分析”焦点经稳定等待后真实截图确认：主舞台 `display:none`，证据区占满 1440×730 内容区，无截图 2 的三块挤压。
- 真实 SDK 相机诊断确认：旧等轴距离 `2.12x` 在重排后的宽画布中使竖向体盒占高约 90%；新构图采用更俯视的 `2.32x`，通过高度 58%–76% 与宽度 >=24% 双门。
- SDK `.sm-zoombar` 拖到底后相机距离仍约 `2.51x` 且未越界，但画面全黑，排除“拉得过远”；持续在 `camera.changed` 中强制 lookAt 会与控件竞争。最终改为拖动结束后一次性重新锚定成果中心，真实像素门通过。
- 单次滚轮真实距离变化约 8.9%，已按产品“约 8%”收敛为 6%–10% 明确区间；缩放条与滚轮继续共享跨度相关上下限。
- 首页中断准备 live 门使用公开 UI 上传 CSV，未执行映射便返回首页；用户案例可见，选中后主动作显示“继续数据准备”，点击准确回到原 `caseId/datasetId/prepare`。后端返回的 `/#/...` 已由 `appRoute()` 规范化。
- 1440x900 全览和分析聚焦、390x844 分析聚焦均实测 `documentElement.scrollWidth === innerWidth`；移动端深度图与明细表采用模块内部横向滚动，不撑宽页面。
- 切片模式实测 `ge-slice-heatmap=1`、旧 `slice-analysis=0`，三维控制保留 X/Y/Z 与索引，首次分析响应自动切换到底部“切片分析”。
- 正确注入隔离 `GEOMODELING_DATA_DIR` 后，`result-analysis-live.spec.ts + platform-live.spec.ts` 共 5 条真实链路通过；首次无环境变量的失败是规格预期的运行库保护，不是产品回归。

## 2026-08-13 第二轮成果工作台反馈

- 控制模式原先把 `520px` 控制列与降亮度画布组合，导致空且不连贯；改为 `348px` 紧凑控制台和正常亮度可交互画布。
- 辅助采样点使用原生 checkbox，其他四项使用 Element Plus checkbox，真实测量存在基线偏移；统一组件后五行输入中心与文字中心误差小于 `0.01px`。
- 分析模式的证据区本就占满可用空间，展开/收起按钮没有可见效果；分析聚焦时移除该按钮。
- 数据与导出页的输入分析嵌在左侧摘要卡，导致左列无限变高；已拆成跨栏独立模块，分析聚焦时自动展开，普通全览仍折叠。
- ready 资产的刷新按钮原本是画布上方孤立动作且无反馈；已并入状态栏，提供“正在刷新…”和“状态已更新”。
- 控制模式移除与默认视角重复的“等轴”，保留三正交视角，并将“恢复默认视角”设为整行主按钮。
- `FRAME_BOOT_SDK_MISSING` 原本发生在 `bootValidated=true` 前，ERROR 无法发送父页，只剩 iframe 技术错误和 30 秒超时；现于 request_id 校验后开放同源协议上报，父页即时显示可理解恢复卡并可原位重建 iframe。
- 模型比较现前置说明同一数据版本、相同验证规则和相同公共有效样本；真实不兼容结果显示“公共有效样本数量不同/验证集比例不同”，不暴露内部字段。
- 唯一 8000 的 1440×900 与 390×844 实测均 `scrollWidth === innerWidth`；比较页、成果控制与五个分析模块无页面级横向溢出。
