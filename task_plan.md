# v0.9.0 视觉产品问题清单实施计划

## 目标

落实 `docs/superpowers/specs/2026-08-11-v6-global-ui-consolidation-design.md` 中 23 项问题，优先消除崩溃、伪交互和错误图表，再统一全站界面并通过真实浏览器视觉门。

## 阶段

| 阶段 | 状态 | 内容 |
|---|---|---|
| 1 | complete | 现状与根因审计；建立可复现测试 |
| 2 | in_progress | 修复 3D 标注、双轴图表、案例工作台标签 |
| 3 | pending | 统一全局壳、术语、页面网格、字号与用户文案 |
| 4 | pending | 清理首页调试信息并复核成果页工具/研判/证据窗 |
| 5 | pending | 单元、类型、构建、Mock E2E 与真实浏览器多视口验收 |
| 6 | pending | 更新问题状态、提交并推送；PR 保持 OPEN |

## 2026-08-12 插入审查任务

| 阶段 | 状态 | 内容 |
|---|---|---|
| A | complete | 恢复当前分支 8000 服务，确认真实案例与成果身份 |
| B | complete | 逐页桌面巡检全部主路由 |
| C | complete | 首页、案例、分析、成果、回收站等关键页移动端巡检 |
| D | complete | 汇总全局导航、流程、视觉层级、技术字段与运行时错误 |

## 2026-08-12 成果工作台与数据准备修复

| 阶段 | 状态 | 内容 |
|---|---|---|
| E | complete | 复现双切片、分析布局、环图遮挡、相机缩放与继续准备死按钮 |
| F | complete | 合并为单一切片工作流并重排成果网格四模块 |
| G | complete | 修复环图、成果页多视口布局和数据准备主动作 |
| H | complete | 限制相机滚轮步长与导航缩放范围，补真实 SDK 视觉门 |
| I | complete | 覆盖 8000，完成桌面/窄屏/移动端和全量测试验收 |

## 约束

- 不覆盖用户或其他 Agent 的未提交改动。
- 不合并 PR，不打标签，不创建 Release。
- 每个修复先有失败测试或稳定复现证据。
- 不把不可用能力做成可点击假按钮。
- 不泄露或提交 DeepSeek 密钥。

## Errors Encountered

| Error | Attempt | Resolution |
|---|---|---|
| `rg: unrecognized flag --s1-font...` | 1 | 搜索模式以 `--` 开头被当作参数；后续使用 `rg -e`。 |
| 计划文档末尾空行被 `git diff --check` 报告，但 PowerShell 命令链仍继续提交 | 1 | 删除末尾空行并 amend；后续显式检查 `$LASTEXITCODE` 后再提交。 |
| 深度格式测试期望固定 1 位小数，但通用 `trimFixed` 删除了 `.0` | 1 | 深度范围使用专用 `toFixed(1)`，其他指标继续按需去尾零。 |
| PowerShell 将双引号正则中的括号与管道误解析为命令 | 1 | `rg` 复杂正则改用 PowerShell 单引号包裹。 |
| live 门连续读取旧 iframe 哈希 `552997f3...` | 1 | 先执行 production build，确认内容哈希变化后再判断运行时。 |
| 初始相机距离合规但新宽画布中体盒占高 90% | 1 | 真实诊断确认旧等轴构图不适配新画幅；改为更俯视的 `2.32x` 构图并保留像素双门。 |
| `camera.changed` 持续纠偏与 SDK 缩放条竞争，体图层无法稳定绘制 | 1 | changed 仅做距离限幅；缩放条 mouseup 后一次性以最终距离重新锚定成果中心。 |
| 首页续接 live 测试按案例名筛选动作按钮，定位器找不到元素 | 1 | 无障碍快照证明案例已显示；改从准备 URL 提取 caseId，用既有 `data-case-id` 精确定位。 |
| 首次手工调用 live E2E 未设置 `GEOMODELING_DATA_DIR`，规格在 beforeAll 主动拒绝 | 1 | 改用 GUID 临时隔离目录重跑；5 条真实链路全部通过，默认 8000 演示库未参与测试。 |
