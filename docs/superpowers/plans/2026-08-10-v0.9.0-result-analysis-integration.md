# v0.9.0 Result Analysis and 3D Anomaly Linkage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Replace prototype-only numbers with deterministic analysis of the materialized result grid and link real anomaly components, slices, evidence, and findings to the native SuperMap3D result workspace.

**Architecture:** Add a pure result-analysis service over existing materialized GridResult artifacts, expose a read-only result summary API, and extend authoritative slice statistics with shared grid thresholds. Reuse the existing anomaly kernel, then extend iframe protocol v2 with typed annotations and camera commands so the result page can drive and receive real spatial selections without implicit database writes.

**Tech Stack:** Python 3.12, NumPy, SciPy, Pydantic, FastAPI, Vue 3, TypeScript, Element Plus, ECharts 5, SuperMap3D 12.1, pytest, Vitest, Playwright.

---

## 0. Execution rules

- Work only in D:\study\Contest\Supermap\.worktrees\v0.9.0-visual-product on feat/v0.9.0-visual-product.
- Do not merge PR #19, tag, release, delete branches, or modify frozen source data.
- Do not reuse static values or prose from geological-analysis-workbench-v6.html.
- Every production change follows RED, GREEN, REFACTOR and gets a focused commit.
- GET routes remain query-only. Saved professional anomalies remain an explicit POST operation.
- Input-sample and result-grid statistics use distinct DTOs and visible labels.
- Existing Volume, Slice, Contour, NetCDF, materialization, formal selection, and professional evidence contracts must not regress.

## 1. File map

New backend files:

- src/geomodeling/platform/result_analysis_contracts.py: strict DTOs and calculation version.
- src/geomodeling/platform/result_analysis.py: statistics, depth bins, component preview and findings.
- src/geomodeling/api/routes/result_analysis.py: read-only API and bounded cache.
- tests/test_result_analysis.py and tests/test_result_analysis_api.py.

Existing backend files:

- src/geomodeling/api/app.py
- src/geomodeling/platform/slice_analysis.py
- tests/test_slice_analysis.py
- tests/test_rendering_api.py

New frontend files:

- web/src/components/results/ResultInterpretationPanel.vue
- web/src/components/results/ResultGridEvidence.vue
- matching tests under web/src/components/results/__tests__/

Existing frontend/iframe files:

- web/src/api/types.ts and client.ts
- web/src/views/ResultWorkbenchView.vue
- web/src/components/results/ResultAnalysisWorkbench.vue
- web/src/components/rendering/NativeVolumePanel.vue
- web/src/components/rendering/VolumeRenderToolbar.vue
- web/src/components/rendering/SuperMapVolumeFrame.vue
- web/src/components/rendering/renderProtocol.ts
- web/public/supermap-volume-frame/app.js

## Task 1: Strict contracts

**Files:** create result_analysis_contracts.py and test_result_analysis.py.

- [ ] Write a failing test importing ResultAnalysisSummary, validating a minimal response, rejecting NaN and unknown fields.

    def test_result_analysis_contract_rejects_non_finite_and_extra():
        payload = minimal_result_summary()
        payload["grid"]["mean"] = float("nan")
        with pytest.raises(ValidationError):
            ResultAnalysisSummary.model_validate(payload)
        payload = minimal_result_summary()
        payload["unexpected"] = True
        with pytest.raises(ValidationError):
            ResultAnalysisSummary.model_validate(payload)

- [ ] Run python -m pytest tests/test_result_analysis.py -q.
  Expected RED: module import fails.

- [ ] Implement RESULT_ANALYSIS_VERSION = "result_analysis.v1" and frozen ContractModel subclasses for identity, variable, grid statistics, thresholds, composition, depth bins/status, components, model evidence, spatial targets, findings and provenance.

- [ ] Re-run the test; expected PASS.

- [ ] Commit:

    git add src/geomodeling/platform/result_analysis_contracts.py tests/test_result_analysis.py
    git commit -m "feat: define result analysis contracts"

## Task 2: Result-grid statistics and depth evidence

**Files:** create result_analysis.py; modify test_result_analysis.py.

- [ ] Write a failing test over a small 3D GridResult with one NoData node. Assert finite-only quartiles, exact low/normal/high counts, ratios summing to one, non-overlapping depth bins and every valid node assigned once.

    summary = analyze_result_grid(
        grid,
        result_id="r1",
        grid_sha256="a" * 64,
        variable_name="RHO",
        variable_unit="ohm_m",
        depth_bins=2,
        component_limit=8,
        min_support_nodes=1,
    )
    assert summary.grid.valid_count == 7
    assert sum(item.count for item in summary.composition.buckets) == 7
    assert sum(item.valid_count for item in summary.depth_profile.bins) == 7

- [ ] Add a 2D test expecting depth status not_applicable and a zero-valid test expecting RESULT_ANALYSIS_NO_VALID_CELLS.

- [ ] Run the target test; expected RED because analyze_result_grid is missing.

- [ ] Implement finite_valid_values, result_thresholds using NumPy linear quantiles, composition_summary, depth_profile and analyze_result_grid. Use low <= p25, high >= p75 and normal strictly between.

- [ ] Run tests; expected PASS. Commit as feat: compute result grid statistics and depth evidence.

## Task 3: Connected components and findings

**Files:** modify result_analysis.py and test_result_analysis.py.

- [ ] Write a failing test with two separated high-value 3D regions. Assert six-neighbor separation, support-descending A/B labels, extrema, centroids, bounds and boundary contact.

- [ ] Write a failing finding test requiring only dominant_depth_interval, largest_high_component, boundary_contact, formal_model and uncertainty_availability. Each finding has numeric evidence, grid hash, confidence and limitations.

- [ ] Run target tests; expected RED because components/findings are empty.

- [ ] Build AnomalyExtractionSpec(direction="high", threshold=p75, min_support_nodes=...), call existing extract_anomalies, sort by support_measure descending then value_max descending then component_id ascending, cap preview and assign A to Z. Do not create an AnomalyExtractionRecord.

- [ ] Run test_result_analysis.py and test_anomaly_extraction.py; expected PASS. Commit as feat: add result anomaly preview and findings.

## Task 4: Query-only API

**Files:** create api/routes/result_analysis.py and test_result_analysis_api.py; modify api/app.py.

- [ ] Write failing API tests:
  - materialized result returns 200 and exact result_id/grid_sha256;
  - depth_bins outside 2 to 32 and component_limit outside 1 to 20 are rejected;
  - unmaterialized result creates no artifact;
  - corrupt grid fails closed;
  - repeated requests for one grid identity are byte-equivalent.

- [ ] Run test_result_analysis_api.py; expected RED with route 404.

- [ ] Add GET /api/results/{result_id}/analysis-summary. Use read_materialized_metadata and load_grid. Add lru_cache(maxsize=32) keyed by result ID, grid hash, version and parameters. Never convert PlatformError to partial success.

- [ ] Register the router after results and before rendering.

- [ ] Run API and result regression tests; expected PASS. Commit as feat: expose read-only result analysis summary.

## Task 5: Shared-threshold slice composition

**Files:** modify slice_analysis.py, test_slice_analysis.py, test_rendering_api.py and frontend SliceStatistics type.

- [ ] Write a failing test asserting thresholds, low/normal/high counts and ratios use p25/p75 of the complete grid rather than slice-local quartiles. Counts equal valid_count.

- [ ] Assert old count/min/max/mean/std/p10/p50/p90 fields are unchanged.

- [ ] Run slice tests; expected RED because new fields are absent.

- [ ] Reuse finite_valid_values and result_thresholds. Pass full-grid thresholds into slice statistics and add fields without renaming existing fields.

- [ ] Run slice/rendering API tests; expected PASS. Commit as feat: add result-threshold slice composition.

## Task 6: Frontend DTOs and evidence

**Files:** modify api/types.ts and api/client.ts; create ResultInterpretationPanel.vue, ResultGridEvidence.vue and tests.

- [ ] Write failing tests with two component rows and one depth finding. Assert focus-component emission, explicit “成果网格” labeling, typed empty states, and no storage/danger/safety/confirmed-boundary claims.

- [ ] Run npm --prefix web run test:unit -- ResultInterpretationPanel ResultGridEvidence.
  Expected RED: imports fail.

- [ ] Add exact ResultAnalysisSummary, ResultAnalysisFinding, ResultAnomalyComponent, ResultDepthBin and extended SliceStatistics types.

- [ ] Add a client function using getJson with path "/results/" + resultId + "/analysis-summary".

- [ ] Implement semantic finding/component HTML and ECharts composition/depth charts. Vue must not recompute thresholds, order or conclusion prose. Dispose charts on unmount.

- [ ] Run targeted Vitest and type-check; expected PASS. Commit as feat: add result analysis evidence components.

## Task 7: Annotation and camera protocol

**Files:** modify renderProtocol.ts, SuperMapVolumeFrame.vue and tests.

- [ ] Write failing tests for finite local positions/bounds, duplicate IDs, negative support, stale revisions, SET_CAMERA_PRESET, FOCUS_ANNOTATION and ANNOTATION_SELECTED origin/source/request checks.

    const annotation = {
      id: "component-1",
      label: "A",
      localPosition: [1, 2, 3],
      bounds: [[0, 2], [1, 3], [2, 4]],
      valueMax: 10,
      supportMeasure: 8,
      supportUnit: "volume_coordinate_unit3",
      color: "#64dab1",
      visible: true,
    }

- [ ] Run renderProtocol and SuperMapVolumeFrame tests; expected RED.

- [ ] Add optional AnnotationWire list, focusedAnnotationId, and CameraPreset union of isometric, top-xy, front-xz and front-yz. Add typed parent commands and child selection receipt. Keep fields optional for v2 compatibility.

- [ ] Expose camera and focus methods from SuperMapVolumeFrame. Run tests and type-check; expected PASS. Commit as feat: extend volume protocol for anomaly linkage.

## Task 8: Iframe scene layers and camera aids

**Files:** modify web/public/supermap-volume-frame/app.js and rendering tests/E2E.

- [ ] Add failing tests requiring dedicated point/line/label collections, reuse of the existing display transform, illegal-coordinate rejection, selection receipts and four camera paths.

- [ ] Run targets; expected RED.

- [ ] Implement PointPrimitiveCollection, PolylineCollection and LabelCollection. Convert centroids using the same local-to-display transform as the volume. Rebuild per revision, highlight focused ID and dispose on reload/error/unmount.

- [ ] Add optional XYZ axes and Z ticks behind bounding-box visibility. Implement deterministic camera positions.

- [ ] Run unit/mock rendering gates; expected PASS. Commit as feat: render result anomalies and camera aids.

## Task 9: Workbench integration

**Files:** modify ResultWorkbenchView.vue, ResultAnalysisWorkbench.vue, NativeVolumePanel.vue, VolumeRenderToolbar.vue and tests.

- [ ] Write failing tests asserting result analysis is fetched by result_id, stale analysis clears before identity change, component clicks focus matching annotations, iframe selection updates interpretation, slice responses update current-slice evidence, and failures do not retain old values.

- [ ] Run targeted workbench/rendering tests; expected RED.

- [ ] Fetch dataset and result summaries separately. Pass components to NativeVolumePanel, store authoritative SliceAnalysisResponse, render new interpretation/evidence components, and retain sample evidence under an explicit input-sample label.

- [ ] Add camera presets and annotation visibility to the real toolbar. Do not add draggable panels.

- [ ] Run targeted Vitest, type-check and build; expected PASS. Commit as feat: integrate result analysis with native volume workspace.

## Task 10: Browser gates and final verification

**Files:** add Mock/Live E2E specs; update acceptance, architecture, current-status and evidence docs.

- [ ] Write the failing browser gate first. It opens a real materialized resistivity result and verifies result/grid identity, real component data when supported, component to scene selection, current-slice ratios, three render modes and zero console/page/unexplained-network failures.

- [ ] Add a repository scan assertion that prototype-only business numbers are absent from product components.

- [ ] Run the new gate against pre-integration behavior and record the expected failure in progress.md.

- [ ] Fix only evidence-driven bugs and add a focused test before every fix.

- [ ] Run the full matrix:

    python -m pytest tests -q -m "not local_data"
    npm --prefix web run test:unit
    npm --prefix web run type-check
    npm --prefix web run build
    npm --prefix web run test:e2e
    npm --prefix web run test:e2e:live -- result-analysis-live.spec.ts
    git diff --check origin/main...HEAD

- [ ] Update docs with the input/result distinction, preview/saved distinction, mutually exclusive modes, support-measure naming and prohibited claims. Commit as docs: verify result analysis and 3d anomaly linkage.

## 2. Spec coverage self-review

| Design requirement | Task |
| --- | --- |
| Result identity and deterministic summary | 1 to 4 |
| Full-grid composition and depth | 2 and 4 |
| Shared current-slice thresholds | 5 |
| A/B/C preview without implicit save | 3 |
| Structured findings and limits | 3, 6, 9 |
| 3D selection linkage | 7 to 9 |
| Axes, depth aids and presets | 7 to 9 |
| Evidence and interpretation | 6 and 9 |
| Fail-closed, no static fallback | 1 to 10 |
| Real browser acceptance | 10 |

The implementation plan contains no unresolved placeholder and no unspecified test step.
